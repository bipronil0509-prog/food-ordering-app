// Vertical shipping-tracker-style timeline: submitted -> under review -> in progress -> resolved
export default function StatusTimeline({ reportId }: { reportId: string }) {
  return null; // TODO
}
