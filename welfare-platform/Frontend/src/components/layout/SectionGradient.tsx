// Wrapper that applies pastel gradient section-break backgrounds (mint -> cream -> blush)
export default function SectionGradient({ children }: { children: React.ReactNode }) {
  return <div>{children}</div>; // TODO
}
