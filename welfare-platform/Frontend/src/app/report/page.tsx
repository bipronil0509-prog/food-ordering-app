// Page 4 — Report an Issue (/report)
// Category picker (Harassment / Corruption / Civic Issue / Safety) + form + confirmation screen
import ReportForm from "@/components/report/ReportForm";

export default function ReportPage() {
  return <ReportForm />;
}
