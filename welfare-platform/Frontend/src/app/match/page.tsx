// Page 2 — Scheme Matcher (/match)
// Multi-step form: income -> occupation/state -> demographics, with progress bar
import MultiStepForm from "@/components/match/MultiStepForm";

export default function MatchPage() {
  return <MultiStepForm />;
}
